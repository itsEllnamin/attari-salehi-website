from django.urls import path
from . import views


app_name = 'orders'

urlpatterns = [
    # path('ajax_admin/', views.get_feature_digital_values, name='feature_digital_values'),
    # path('cheapest/', views.CheapestProductsView.as_view(), name='cheapest'),
    # path('newest/', views.NewestProductsView.as_view(), name='newest'),
    # path('categories/', views.CategoriesView.as_view(), name='categories'),
    # path('product/<slug:slug>/', views.ProductView.as_view(), name='product'),
    # path('related/<slug:slug>/', views.RelatedProductsView.as_view(), name='related'),
    # path('product_category/<slug:slug>/', views.ProductCategoryView.as_view(), name='product_category'),
    # path('category_products/<slug:slug>/', views.CategoryProductsView.as_view(), name='category_products'),
]
